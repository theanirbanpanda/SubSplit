// OccupancyCard — seat info now lives in the Listing Details accordion (left column)
// and the "N seats remaining" line in the buy panel (right column).
// Kept as file to avoid breaking any imports; renders nothing.
function OccupancyCard() {
  return null;
}

export default OccupancyCard;
